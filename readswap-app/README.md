  # ReadSwap-App

  To preview and run the project on your device:
  1) Download the project folder `readswap-app`
  2) Open `readswap-app` directory in vscode
  3) In the terminal, run `npm install`
  4) Run `npm start` to view project in browser

  5) * Please delete `node_modules` folder before sending updates to GitHub 
  